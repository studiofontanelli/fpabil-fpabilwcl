import { SalvaRichiestaRequest } from './salva-richiesta-request.model';

describe('SalvaRichiestaRequest', () => {
  it('should create an instance', () => {
    expect(new SalvaRichiestaRequest()).toBeTruthy();
  });
});
